package application.MapReduce;

import java.io.File;

import application.DFS.*;

public class RunMapReduce {

	public void runMapReduce(File fileInput, File fileOutput)
	{
		int size = 0;
		long guid;
		//Locating a successor?? 
		//Obtain the number of nodes
	//	chord.successor.onNetworkSize(guid, 1);
		
		//Obtained from onNetworkSize after a full cycle
	//	wait until size > 0
	//	interval = 1936 / size;
		
	//	createFile(fileOutput + ".map", interval, size);
	
		
		
	}
}
